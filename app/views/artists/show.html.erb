<div class="bump-down">
  <div class="row">
    <div class="col-md-12">
      <div class="well">
        <h1><%= @artist.name %></h1>
        <%= image_tag @artist.avatar.url(:medium) %>
  	<% @artist.songs.each do |song| %>
  	  <h3><%= link_to song.name, song %></a></h3>
  	<% end %>
      </div>
    </div>
  </div>
</div>
